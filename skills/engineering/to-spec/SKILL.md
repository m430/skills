---
name: to-spec
description: "把当前对话整理成规格（spec）并发布到项目的 issue tracker：不做访谈，只综合你们已经讨论过的内容。"
disable-model-invocation: true
---

本技能取当前对话上下文和对代码库的理解，产出一份规格（spec）。不要访谈用户，只综合你已经知道的内容。

issue tracker 和 triage 标签词汇表应当已经提供给你。如果没有，让用户运行 `/setup-matt-pocock-skills`。

## 流程

1. 如果还没探索过仓库，先探索以了解代码库的当前状态。在整个规格中使用项目领域词汇表的词汇，并尊重你所触及区域内的任何 ADR。

2. 勾勒你打算在哪些接缝（seam）上测试该功能。优先使用已有接缝而非新接缝。尽可能用最高的接缝。如果确实需要新接缝，在你能做到的最高点提出。整个代码库的接缝越少越好：理想数量是一。

与用户确认这些接缝符合他们的预期。

3. 使用下面的模板撰写规格，然后发布到项目的 issue tracker。打上 `ready-for-agent` triage 标签，无需额外的 triage。

<spec-template>

## 问题陈述

用户面临的问题，从用户的视角描述。

## 解决方案

问题的解决方案，从用户的视角描述。

## 用户故事

一份很长的、编号的用户故事列表。每个用户故事应采用如下格式：

1. 作为 <角色>，我想要 <功能>，以便 <收益>

<user-story-example>
1. 作为手机银行客户，我希望查看账户余额，以便对我的支出做出更有依据的决策
</user-story-example>

这份用户故事列表应当极其详尽，覆盖该功能的方方面面。

## 实现决策

已经做出的实现决策的列表。可以包括：

- 将要构建/修改的模块
- 这些模块中将要修改的接口
- 来自开发者的技术澄清
- 架构决策
- Schema 变更
- API 契约
- 具体交互

不要包含具体文件路径或代码片段。它们很快就会过时。

例外：如果原型产出了一段比散文更能精确表达某个决策的片段（状态机、reducer、schema、类型形状），把它内联到相关决策里，并简要注明它来自原型。裁剪到只剩富含决策的部分即可，不必是能跑的 demo，只要关键片段。

## 测试决策

已经做出的测试决策的列表。包括：

- 描述什么样的测试算好测试（只测外部行为，不测实现细节）
- 哪些模块将被测试
- 测试的先例（即代码库中类似类型的测试）

## 范围之外

对本规格范围之外事项的描述。

## 补充说明

关于该功能的任何其他说明。

</spec-template>
